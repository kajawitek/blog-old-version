<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "Blog", root_path, class:"navbar-brand" %>
        </li>
        <% if author_signed_in? %>
          <li class="nav-item">
            <%= link_to "Sign Out", destroy_author_session_path, method: :delete, class:"navbar-brand" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Register", new_author_registration_path, class:"navbar-brand" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign In", new_author_session_path, class:"navbar-brand" %>
          </li>
        <% end %>
      </ul>
      <% if author_signed_in? %>
        <ul class="navbar-nav ml-auto mb-2 mb-lg-0"">
          <span class="navbar-text">
            <%= "Hi #{current_author.email}!" %>
          </span>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
